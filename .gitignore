bin/
obj/
publish/
package/
*.zip
*.user
.vs/
.claude
claude-memory/

# Configuration dumps taken from a running server. They are POST bodies from testing, they
# carry the playback account's password, and one of them was published for a day before
# anybody noticed - which is the whole reason this line exists.
cfg*.json
chan*.json

# Local build output and release staging. Every released ZIP lives on GitHub; these are
# throwaway copies of it.
publish-*/
package-*/
release-package-*/
release-github-*/
release-candidate-*/
.release-check-*/

# Screenshots taken off the emulator while testing.
emulator-*.png

# Caches from tools run out of this folder.
.gradle-user/
.pnpm-store/
.android-user/
